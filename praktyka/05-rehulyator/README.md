# Робота 5. Від «якщо» до регулятора

Тиждень 5 · до **5 балів** · на парі — до 14:15 · не були на парі — до початку наступного заняття

---

## Мета

Зробити одну задачу — **рух за лінією** — трьома способами: релейно, пропорційно
і з завеликим коефіцієнтом. Порівняти **числами**: час до кінця траси й те,
як виглядає рух. І пояснити, чому ви зупинились саме на своєму `Kp`.

⭐ «Зробити сильніше» не означає «зробити краще». Сьогодні ви побачите це очима.

---

## Мінімум для зарахування

- [ ] `releynyi.py` і `proportsiynyi.py` у цій теці
- [ ] `zvit.md` — таблиця: реле й обраний `Kp` — **по три прогони**, плюс `Kp`, за якого починається розгойдування
- [ ] три речення: чому ваш `Kp` саме такий
- [ ] рядок у `journal.md`, тиждень 5
- [ ] коміт і `git push`

## Контрольні точки на парі

| Час | Має бути готово | Що написати в чат |
|---|---|---|
| 12:45 | кроки 0–2 | `реле: 86 / 84 / 88 с` — три часи |
| 13:30 | кроки 3–4 | `Kp …: … с · розгойдування з Kp …` |
| 14:00 | — | номер кроку, на якому ви зараз |
| 14:15 | крок 5, push | посилання на свій `zvit.md` |

**Перші четверо**, хто напише в чат рядок після кроку 4, показують роботу на розборі:
двоє з найшвидшим часом і двоє з найспокійнішим рухом.

---

## Карта кроків

| Крок | Що | Хв |
|---|---|---|
| 0 | траса, каркас | 10 |
| 1 | прогноз | 5 |
| 2 | реле, три прогони | 20 |
| 3 | П-регулятор, підбір `Kp` | 25 |
| 4 | межа: з якого `Kp` розгойдується | 15 |
| 5 | три прогони з обраним `Kp`, таблиця, коміт | 25 |
| ⭐ | драбина й треки — хто закінчив раніше | решта |

---

## Крок 0. Траса й каркас — 10 хв

**Світ:** Worlds → Select World → **Line Following Challenges**, траса **Simple Curves**.
Це не коло, а смуга з поворотами: робот стартує на лінії, і міряємо **час до кінця траси**.

```python
from ev3dev2.motor import MoveTank, OUTPUT_A, OUTPUT_B, SpeedPercent
from ev3dev2.sensor import INPUT_1, INPUT_4
from ev3dev2.sensor.lego import ColorSensor
from ev3dev2.sensor.virtual import GPSSensor
import time

tank = MoveTank(OUTPUT_A, OUTPUT_B)
color = ColorSensor(INPUT_1)          # ⚠️ у Gears порт обов'язковий
gps = GPSSensor(INPUT_4)              # лише щоб зупинити секундомір на фініші
time.sleep(0.5)

PORIH = 50                            # з роботи 4: біле 100, чорне 1


def obmezhyty(v):
    """SpeedPercent поза ±100 у Gears падає з ValueError."""
    return max(-100, min(100, v))


pochatok = time.time()
while time.time() - pochatok < 120 and gps.y < 85:
    v = color.reflected_light_intensity
    # ↓↓↓ сюди — код кроку 2, 3 або 4 ↓↓↓

tank.off()
print("час до кінця траси: %.1f с" % (time.time() - pochatok))
```

⚠️ GPS тут — лише секундомір. У самому керуванні його **не використовувати**.

✔ Каркас без коду в циклі одразу друкує час (робот стоїть) — значить, усе підключено.

---

## Крок 1. Прогноз — 5 хв

У `zvit.md`: у скільки разів, на вашу думку, П-регулятор пройде трасу швидше за реле?

✔ Число записане **до** запусків.

---

## Крок 2. Релейний варіант, три прогони — 20 хв

Вставити в цикл:

```python
    if v < PORIH:
        tank.on(SpeedPercent(10), SpeedPercent(0))
    else:
        tank.on(SpeedPercent(0), SpeedPercent(10))
```

Запустити **три рази**, записати час кожного. Зберегти як `releynyi.py`.

✔ Робот доїжджає до кінця траси, **виляючи й повільно** — це нормально: команда має
лише два значення, тому помилка 1 і помилка 40 обробляються однаково.

---

## Крок 3. Пропорційний регулятор — 25 хв

Замінити код у циклі на:

```python
    e = PORIH - v
    korekciya = Kp * e
    tank.on(SpeedPercent(obmezhyty(BAZA - korekciya)),
            SpeedPercent(obmezhyty(BAZA + korekciya)))
```

А **перед циклом** додати:

```python
BAZA = 20
Kp = 0.5
```

Підбирати `Kp`: підіймати, доки робот не почне **розгойдуватись**, потім узяти приблизно
половину. **Кожне спробуване значення — рядок у таблиці.** Зберегти як `proportsiynyi.py`.

✔ Робот доїжджає до кінця траси помітно швидше, ніж у кроці 2, і виляє менше.

---

## Крок 4. Знайти межу — 15 хв

Значення `Kp`, за якого робот починає **розгойдуватись** — перелітати через лінію
туди-сюди й злітати з траси. Записати його.

✔ У таблиці є рядок «Kp …: не доїхав — розгойдування».

---

## Крок 5. Три прогони, таблиця, коміт — 25 хв

Обраний `Kp` — **три прогони**. Таблиця в `zvit.md` і три речення: чому саме цей `Kp`.
Рядок у `journal.md`, потім `git add .`, `git commit -m "Робота 5: реле і П-регулятор"`, `git push`.

✔ На GitHub видно обидва `.py` і `zvit.md`.

---

## Заготовка `zvit.md`

```markdown
# Робота 5 — реле і П-регулятор

Прогноз (крок 1): П швидше в ___ разів

| Варіант | Kp | Час до кінця траси, с (3 прогони) | Як виглядає рух |
|---|---|---|---|
| релейний | — | | |
| пропорційний | | | |
| … усі спробувані Kp … | | | |
| Kp завеликий | | — | розгойдування |

## Чому мій Kp саме такий
1.
2.
3.
```

---

## Якщо щось пішло не так

| Що бачите | Причина | Що зробити |
|---|---|---|
| `TypeError: No color sensor connected to null` | датчик без порту | `ColorSensor(INPUT_1)` |
| `ValueError: Value must be between -100 and 100` | швидкість вийшла за 100 % | загорнути в `obmezhyty(...)` |
| робот одразу з'їжджає з лінії й їде геть | `Kp` замалий або знак навпаки | перевірити: `e = PORIH - v`, ліве `BAZA - korekciya` |
| робот крутиться на місці біля старту | `Kp` завеликий | зменшити вдвічі |
| цикл не закінчується | робот загубив лінію й не дійде до фінішу | зупинити ■, записати «не доїхав» — це теж результат |
| `gps.y` не росте | інший світ або інша траса | Line Following Challenges → Simple Curves |

---

## ⭐ Драбина — хто закінчив раніше

| # | Завдання | Хв |
|---|---|---|
| ⭐1 | **Швидке реле.** Реле на 20/5 замість 10/0 — де саме робот губить лінію і чому? | 10 |
| ⭐2 | **Слабкий мотор.** Помножити команду одного колеса на 0,95. Чи П-регулятор компенсує зміщення сам, чи потрібна І-складова? | 15 |
| ⭐3 | **Крива помилки.** Надрукувати `e` за 5 секунд для П і для П+Д (трек П) і порівняти, яка спокійніша | 20 |
| ⭐⭐4 | **Складна траса.** Та сама задача на трасі **Sharp Turns**. Знайти `BAZA` і `Kp`, з якими робот проходить її до кінця, і пояснити, чому числа з Simple Curves тут не підійшли | 40+ |

## Трек П

**D-складова** — у циклі замість кроку 3:

```python
    e = PORIH - v
    d = e - e_pop
    korekciya = Kp * e + Kd * d
    e_pop = e
    tank.on(SpeedPercent(obmezhyty(BAZA - korekciya)),
            SpeedPercent(obmezhyty(BAZA + korekciya)))
```

(перед циклом: `Kd = 1.0` і `e_pop = 0`). Таблиця Kp × Kd щонайменше 3 × 3: час і чи
розгойдується. Чесна відповідь «Д тут нічого не дала» — теж відповідь, якщо спирається на числа.

⚠️ **I-складову не беремо.** Вона легко псує все, якщо не обмежити накопичення.

## Трек Д

Критерій інший: **рух має виглядати впевненим, а не смиканим.** Коротке відео трьох варіантів
і опис словами, чим вони відрізняються для стороннього спостерігача. Яке `Kp` виглядає
найспокійніше — і чи збігається воно з найшвидшим?

---

## Не були на парі або не встигли

- ті самі кроки 0–5, дедлайн — **початок наступного заняття**;
- замість показу — у `zvit.md` розділ **«Розбір»**:
  1. Чому реле виляє, навіть коли помилка крихітна?
  2. Що ви побачили, коли `Kp` став завеликим?
  3. Чи збігається найшвидший `Kp` із найспокійнішим — і що це каже про «правильний» `Kp`?
- посилання на `zvit.md` — у чат дисципліни.

---

## RoboMind

**Basics 2 — дедлайн наступного тижня.**

---

## Критерії — 5 балів

| Балів | За що |
|:---:|---|
| 2 | Обидва варіанти працюють, по три прогони, таблиця є |
| 2 | ⭐ Пояснюєте, **чому обрали саме таке Kp** — з опорою на свої заміри |
| 1 | Рядок у `journal.md` |

⚠️ «`Kp` такий, бо так було в прикладі» — це нуль за другий критерій.
