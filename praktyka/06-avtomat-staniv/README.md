# Робота 6. Поведінка як автомат станів

Тиждень 6 · до **5 балів** за роботу + ⭐ **15 балів** за відео місії · на парі — до 14:15 · не були на парі — до початку наступного заняття

---

## Мета

Розкласти **свою місію** на стани, намалювати діаграму й реалізувати 3–4 стани
так, щоб у будь-який момент консоль казала, **що робот робить зараз**.
Зняти 30 секунд відео, де робот виконує частину місії.

⭐ Це найбільший артефакт модуля 1 до захисту: відео, знятого сьогодні, вистачить,
щоб на тижні 9 було що показувати.

---

## Мінімум для зарахування

- [ ] `dijagrama.png` — діаграма станів (можна фото від руки), переходи підписані
- [ ] `misiya.py` — 3–4 стани, **друк кожного переходу**, таймаут щонайменше в одному стані
- [ ] відео до 30 с (файл або посилання) і в `zvit.md` — який фрагмент місії показано
- [ ] рядок у `journal.md`, тиждень 6
- [ ] коміт і `git push`

## Контрольні точки на парі

| Час | Має бути готово | Що написати в чат |
|---|---|---|
| 12:45 | кроки 0–2 | назви своїх станів через стрілку: `poshuk → pidhid → …` |
| 13:30 | крок 3 | останній рядок консолі після запуску |
| 14:00 | — | номер кроку, на якому ви зараз |
| 14:15 | крок 6, push | посилання на відео |

**Перші четверо**, хто скине в чат посилання на відео, показують його в третій парі.

---

## Карта кроків

| Крок | Що | Хв |
|---|---|---|
| 0 | каркас автомата | 10 |
| 1 | що робот має вміти — список дій | 10 |
| 2 | діаграма станів | 15 |
| 3 | стани в коді | 30 |
| 4 | таймаут | 10 |
| 5 | своя сцена й відео | 15 |
| 6 | `zvit.md`, журнал, коміт | 10 |
| ⭐ | драбина й треки — хто закінчив раніше | решта |

---

## Крок 0. Каркас — 10 хв

Робот їде до перешкоди, сповільнюється, зупиняється й розвертається. У світі **Grid** працює як є.

```python
from ev3dev2.motor import MoveTank, OUTPUT_A, OUTPUT_B, SpeedPercent
from ev3dev2.sensor import INPUT_2, INPUT_3
from ev3dev2.sensor.lego import UltrasonicSensor, GyroSensor
import time

tank = MoveTank(OUTPUT_A, OUTPUT_B)
ultra = UltrasonicSensor(INPUT_2)     # ⚠️ у Gears порт обов'язковий
gyro = GyroSensor(INPUT_3)
time.sleep(0.5)

stan = "poshuk"
vkhid = time.time()                   # коли увійшли в поточний стан


def perekhid(novyi):
    """Єдине місце, де змінюється стан: друк і дії при вході."""
    global stan, vkhid
    print(f"{stan} -> {novyi}")
    stan = novyi
    vkhid = time.time()
    if novyi == "rozvorot":
        gyro.reset()                  # скинути гіроскоп при вході в поворот


while stan != "gotovo":
    d = ultra.distance_centimeters

    if stan == "poshuk":              # їдемо вперед, дивимось уперед
        tank.on(SpeedPercent(30), SpeedPercent(30))
        if d < 45:
            perekhid("pidhid")

    elif stan == "pidhid":            # наближаємось повільно
        tank.on(SpeedPercent(10), SpeedPercent(10))
        if d < 15:
            perekhid("rozvorot")

    elif stan == "rozvorot":          # розвертаємось на місці
        tank.on(SpeedPercent(20), SpeedPercent(-20))
        if gyro.angle >= 180:
            tank.off()
            perekhid("gotovo")

    if time.time() - vkhid > 20:      # таймаут: стан без виходу
        print("таймаут у стані", stan)
        tank.off()
        break
```

✔ У консолі три рядки: `poshuk -> pidhid`, `pidhid -> rozvorot`, `rozvorot -> gotovo`.

⭐ У кожен момент робот рівно в одному стані, стан змінюється тільки в `perekhid()`, і немає
жодного вкладеного `if` глибше двох рівнів. У цьому вся ідея.

---

## Крок 1. Що робот має вміти — 10 хв

У `zvit.md` — список дій **вашої** місії з картки: шукати, їхати, обʼїжджати, зупинятись,
брати, повертатись… Позначте, яку **частину** місії зробите сьогодні.

✔ Список із 4–8 дій, одна частина позначена.

---

## Крок 2. Діаграма станів — 15 хв

Прямокутники — стани, стрілки — переходи, **на кожній стрілці — умова**. Від руки на папері
й фото — цілком годиться. Файл `dijagrama.png`.

✔ 3–4 стани, у кожного є вихід, кожна стрілка підписана.

---

## Крок 3. Стани в коді — 30 хв

**Мета:** стани й переходи з діаграми — у `misiya.py`, від каркаса. Стан змінювати лише
через `perekhid()`.

✔ Друк у консолі повторює стрілки діаграми в тому самому порядку.

---

## Крок 4. Таймаут — 10 хв

Щонайменше в одному стані — вихід за часом (у каркасі — 20 с). Перевірте навмисно:
зіпсуйте умову переходу й запустіть.

✔ У консолі `таймаут у стані …`, робот зупинився, а не завис назавжди. Поверніть умову.

---

## Крок 5. Своя сцена й відео — 15 хв

1. **Worlds → Load from file** → `kursova/scena/custom_world.json` (сцена з тижня 4).
   Сцени ще немає — світ **Grid**.
2. Запустити й записати екран: **Windows** — Win + Alt + R (Xbox Game Bar),
   **macOS** — Cmd + Shift + 5. Gears відео сам не пише.

✔ Відео до 30 с: видно початок, дію й результат.

---

## Крок 6. `zvit.md`, журнал, коміт — 10 хв

У `zvit.md` — який фрагмент місії на відео і посилання на нього. Рядок у `journal.md`
(стани, що не спрацювало з першого разу). Потім `git add .`,
`git commit -m "Робота 6: автомат станів і відео"`, `git push`.

✔ На GitHub видно `dijagrama.png`, `misiya.py`, `zvit.md`.

---

## Якщо щось пішло не так

| Що бачите | Причина | Що зробити |
|---|---|---|
| `TypeError: No ultrasonic sensor connected to null` | датчик без порту | `UltrasonicSensor(INPUT_2)`, `GyroSensor(INPUT_3)` |
| останній рядок у консолі — `poshuk -> pidhid`, і більше нічого | застряг у стані: умова переходу не виконується | подивитись на умову цього стану; для того й друк |
| `таймаут у стані …` | стан без виходу | це не помилка таймауту — це він вас урятував; виправити умову |
| розворот не закінчується | гіроскоп не скинули або крутиться ліворуч (кут від'ємний) | `gyro.reset()` при вході; швидкості `20, -20` |
| стан міняється щоцикла туди-сюди | умова на межі — тремтіння | гістерезис із роботи 4: два пороги |
| сцена не завантажується | файл не з World Builder | Worlds → World Builder → File → Save world to file |

---

## ⭐ Драбина — хто закінчив раніше

| # | Завдання | Хв |
|---|---|---|
| ⭐1 | **Новий стан без поломки.** Додати стан, якого немає в базовій місії, і перевірити, що жоден старий перехід не зламався | 15 |
| ⭐2 | **Наосліп.** Прибрати друк переходів, зламати умову й шукати причину; потім повернути друк. Скільки часу пішло з ним і без нього? | 15 |
| ⭐3 | **Таблиця переходів.** «Стан × подія → новий стан» на всі комбінації, навіть неможливі. Чи код справді їх ігнорує? | 20 |
| ⭐⭐4 | **Повернутись на старт.** Після дії робот повертається туди, звідки почав: назад **стільки ж обертів коліс**, скільки проїхав уперед (робота 3). Промах, виміряний GPS, — не більше 5 см | 40+ |

## Трек П

**Стан обробки відмови**: робот застряг — відʼїхати назад, повернути, спробувати ще раз.
Порахувати на десяти прогонах, скільки разів цей стан спрацював.

## Трек Д

Діаграма, зрозуміла **людині без технічної освіти**: назви станів словами, а не `stan_2`.
І стан робота, видимий глядачеві без читання коду: рух, пауза, розворот. Покажіть це на відео.

---

## ⭐ Із зірочкою

Знайти в своїй діаграмі стан, **у який робот може потрапити, але з якого не може вийти**, —
глухий кут автомата. Він майже завжди є в першій версії. Опишіть його й додайте вихід.

⚠️ Це те, що на захисті питають першим: «який стан спрацьовує, коли робот застряг?»

---

## Не були на парі або не встигли

- ті самі кроки 0–6, дедлайн — **початок наступного заняття**;
- замість показу — у `zvit.md` розділ **«Розбір»**:
  1. Який стан найчастіше застрягав і чому?
  2. Навіщо друкувати переходи, якщо робот і так видно на екрані?
  3. Що буде з вашим автоматом, якщо додати ще два кроки місії?
- посилання на відео й `zvit.md` — у чат дисципліни.

---

## ⚠️ Дедлайн сьогодні

- [ ] **RoboMind Basics 2 пройдено** — прогрес видно викладачу на платформі

---

## Критерії

### Робота на занятті — 5 балів

| Балів | За що |
|:---:|---|
| 2 | Діаграма є, 3–4 стани реалізовані, переходи друкуються |
| 2 | ⭐ Пояснюєте, **чому переходи саме такі** |
| 1 | Рядок у `journal.md` |

### Відео базової поведінки — 15 балів

| Балів | За що |
|:---:|---|
| 5 | Відео є, до 30 с, робот щось робить |
| 5 | Видно, що це **частина вашої місії**, а не рух узагалі |
| 5 | ⭐ Поведінка відповідає діаграмі: у відео впізнаються стани |
